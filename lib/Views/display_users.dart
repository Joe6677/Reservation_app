import 'package:flutter/widgets.dart';

class DisplayUsers extends StatelessWidget {
  const DisplayUsers({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Display Users"));
  }
}
